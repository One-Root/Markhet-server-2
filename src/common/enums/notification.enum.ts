enum WhatsAppMessageType {
  TEXT = 'text',
  TEMPLATE = 'template',
}

enum NotificationChannel {
  PUSH = 'PUSH',
  SMS = 'SMS',
  WHATSAPP = 'WHATSAPP',
  APP = 'APP',
}

export { NotificationChannel, WhatsAppMessageType };
