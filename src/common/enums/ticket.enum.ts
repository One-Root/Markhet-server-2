enum TicketType {
  CALL_REQUEST = 'CALL_REQUEST',
}

enum TicketStatus {
  OPEN = 'OPEN',
  IN_PROGRESS = 'IN_PROGRESS',
  RESOLVED = 'RESOLVED',
  CLOSED = 'CLOSED',
}

export { TicketType, TicketStatus };
